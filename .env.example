# Tools monorepo — optional global defaults.
# Prefer per-site env vars named in sites.registry.json (e.g. LAPPZVI_WP_APP_PASSWORD).

# Example for lappzvi content API:
# LAPPZVI_WP_USER=
# LAPPZVI_WP_APP_PASSWORD=
