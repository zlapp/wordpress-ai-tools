# Copy to .env locally — never commit real values.
# Names should match site.yaml content.env / tools registry.

{{SITE_ID}}_WP_USER=
{{SITE_ID}}_WP_APP_PASSWORD=

# Deploy (if using SFTP from CI or local scripts)
SFTP_HOST=
SFTP_USER=
SFTP_PASS=
